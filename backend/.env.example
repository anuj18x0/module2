# MongoDB Atlas Configuration
# Get your connection string from MongoDB Atlas Dashboard:
# 1. Go to https://cloud.mongodb.com
# 2. Create a cluster
# 3. Click "Connect" → "Drivers" → Copy connection string
# 4. Replace <password> with your database password
# 5. Replace <username> with your database username

MONGO_URL=mongodb+srv://anuj:Anuj@2406@cluster0.xxxxx.mongodb.net/realty_genie?retryWrites=true&w=majority

# OR for local MongoDB:
# MONGO_URL=mongodb://localhost:27017

# Google Cloud Vertex AI Configuration (for Gemini API)
PROJECT_ID=your-gcp-project-id
LOCATION=us-central1

# Gemini Model Configuration
GEMINI_MODEL=gemini-2.5-flash

# SMTP Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME=Arth Arvind
